<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sky31.gonggong">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".module.main.MainActivity"
            android:theme="@style/ColorTranslucentTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".base.BaseActivity"
            android:theme="@style/ColorTranslucentTheme" />
        <activity
            android:name=".module.login.LoginActivity"
            android:label="@string/login"
            android:theme="@style/ColorTranslucentTheme" />
        <activity
            android:name=".module.search_mate.SearchMateActivity"
            android:theme="@style/ColorTranslucentTheme" />
        <activity android:name=".module.swzl.SwzlActivity" />
        <activity
            android:name=".module.swzl.PublishSwzlActivity"
            android:label="@string/title_activity_publish_swzl" />
        <activity
            android:name=".module.article.list.ArticleListActivity"
            android:label="@string/title_activity_article_list" />
        <activity
            android:name=".module.library.LibraryActivity"
            android:label="@string/library"
            android:theme="@style/ColorTranslucentTheme" />
        <activity android:name=".module.swzl.SwzlDetailActivity"></activity>
        <activity
            android:name=".module.grade.GradeActivity"
            android:label="@string/grade"></activity>
    </application>

</manifest>
